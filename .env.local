# This file stores local environment variables
# Add any sensitive keys here (they won't be committed to Git thanks to .gitignore)

# Example: Email service configuration
# NEXT_PUBLIC_API_URL=http://localhost:3000
